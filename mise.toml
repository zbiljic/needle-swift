[tools]
# Use Swift from the selected Xcode toolchain (Swift 6.3 or newer).
actionlint = "1.7.12"
swiftformat = { version = "0.63.0", os = ["macos"] }
swiftlint = { version = "0.65.1", os = ["macos"] }
